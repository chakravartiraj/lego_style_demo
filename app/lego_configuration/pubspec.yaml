name: lego_configuration
description: Configuration for Lego demo
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.4

dependencies:
  flutter:
    sdk: flutter
  get_it: ^7.2.0
  lego_list:
    path: ../../feature/lego_list

dev_dependencies:

  flutter_lints: ^2.0.0
