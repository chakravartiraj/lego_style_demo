name: core_testing
description: Shared sociable testing utilities (MockDioClient, TestBootstrap) for Lego Monorepo
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.4

dependencies:
  flutter:
    sdk: flutter
  dio: ^5.0.0
  mocktail: ^1.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
